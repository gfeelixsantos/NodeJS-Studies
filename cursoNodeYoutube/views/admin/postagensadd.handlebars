{{#each erros}}
    <div class="alert alert-primary">{{ text }}</div>
    {{else }}
{{/each}}
<h2>Nova Postagem</h2>
<form action="/admin/postagens/new" method="post">
  <div class="mb-3 input-group-sm w-25">
    <label for="nome" class="form-label">Titulo</label>
    <input type="text" class="form-control input-group-sm border-primary-subtle" id="titulo" name="titulo">
  </div>
  <div class="mb-3 input-group-sm w-25">
    <label for="slug" class="form-label">Categoria</label>
    <select type="text" class="form-control border-primary-subtle" id="categoria" name="categoria">
        {{#each listagemCategorias}}
            <option value="{{ nome }}">{{ nome }}</option>
            {{else }}
        {{/each}}
    </select>
  </div>
    <div class="mb-3 input-group-sm w-25">
    <label for="slug" class="form-label">Conteúdo</label>
    <textarea type="text" class="form-control border-primary-subtle" id="conteudo" name="conteudo" rows="2" cols="40"></textarea>
  </div>
  <button type="submit" class="btn btn-success">Postar</button>
</form>